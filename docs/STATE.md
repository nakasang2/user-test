# STATE.md — 作業状態（セッション横断）

> Claude向け運用ルール: セッション開始時にこのファイルを読んでから作業に入る。作業の節目・中断時・ship後に更新する。終わった項目は「完了ログ」へ移し、完了ログは直近5件だけ残す。

- **最終更新**: 2026-07-28（UXレビュー起点の改善が一巡。ブランチ qa/release-readiness）

## 進行中
- なし（UXレビュー起点の改善が一巡。[PR #22](https://github.com/nakasang2/user-test/pull/22) のみマージ待ち）

## 次にやること（再開ポイント）
### ユーザー側の作業
- [PR #22](https://github.com/nakasang2/user-test/pull/22) のマージ（動画上部の帯を撤去）
- インタビュー詳細で「文字起こしから回答を抽出する」を実行（5件ずつ・過去11件なら3回）。抽出内容が元の発言とズレていないか確認
- 実ブラウザで小窓フロー（録画→サービス→達成）と顔フレーミング警告の確認。preview では getDisplayMedia/カメラが使えず未検証

### 未着手の課題（優先度順）
1. **prisma の不整合**: schema.prisma は postgresql、migrations は sqlite。デプロイは `db push` なので現状は動くが、`prisma migrate` を使うと壊れる。方針（sqlite 側を捨てる / postgres で migration を作り直す）の判断が必要
2. **英語の要約が残るセッション**: 日本語化はプロンプト修正済みだが、既存セッションは個別に「AI再分析」しないと英語のまま。テーマ横断集計も英語版と日本語版に分かれる。まとめて再分析する導線が無い
3. **未使用依存 @daily-co/daily-js**: src からの import が 0。`dailyRoomUrl` も InterviewRoom で受け取るだけで未使用。削除候補
4. **AI設計ページから rating/nps を作れない**: 生成される質問が `type: 'open'` 固定
5. **既存の lint エラー 21件**: InterviewRoom / useEmotionDetection などの setState-in-effect。今回の変更分は 0 件
6. 軽微: CreateInterviewModal の htmlFor、gray-400 placeholder のコントラスト

## 注意（並行セッション・未コミット変更）
- なし（prisma の PostgreSQL 移行分はユーザー指示で 2026-07-27 に破棄済み）。

## 完了ログ（直近5件）
- 2026-07-28: セッション詳細のUI整理。会話ログ↔動画↔感情グラフの相互連動、レイアウトを上下2段に（グラフを実質シークバー化）、時系列グラフの重複解消、動画上部の不要な帯を撤去（PR #19〜#22）
- 2026-07-28: recharts 3 で `activePayload` が廃止されており感情グラフのシークが無効だった問題を修正。クリック座標から自前算出する方式に（PR #18）
- 2026-07-28: 過去セッションの回答を文字起こしからAI抽出する導線を追加＋AI要約の日本語化。比較テーブルが空だった原因の解消（PR #17）
- 2026-07-28: UXレビュー P2。調査の編集・横断CSV・共有リンク期限・SEQ・スクリーニング・パイロット実行・所要時間の自動算出（PR #13, #14）
- 2026-07-28: UXレビュー P0/P1。測定結果の構造化（成功率・NPS・所要時間）、発言単位の感情判定の実データ化、全文検索、タグ付け/ハイライト、質問×被験者の比較テーブル（PR #11, #12, #15, #16）
