# STATE.md — 作業状態（セッション横断）

> Claude向け運用ルール: セッション開始時にこのファイルを読んでから作業に入る。作業の節目・中断時・ship後に更新する。終わった項目は「完了ログ」へ移し、完了ログは直近5件だけ残す。

- **最終更新**: 2026-07-29（調査ページの結果サマリーまで完了。PR #24 マージ済み、**PR #25 がマージ待ち**。ブランチ qa/release-readiness）

## 進行中
- **PR #25 がマージ待ち**: 調査ページ最上部の結果サマリー（成功率・NPS・所要時間・完了、最も苦戦したタスク、AI総括）、並びの変更（結果 → 比較 → 個別）、削除済みタスク・質問を集計から外す恒久対応（excludedAt）、参加者画面から感情表示を撤去
  さらに、参加者画面から感情表示を撤去、詰まった参加者への声かけ＋ヒント（記録付き）
  - **このPRには DB スキーマ変更が含まれる**。TaskResult / Answer に `excludedAt`（NULL 許容）、Task に `hint`（NULL 許容）、Interview に `hintDelaySec`（NULL 許容）、TaskResult に `usedHint`（Boolean 既定 false）。いずれも追加のみ。ビルド時の `prisma db push` が `next build` より先に走るので、列が無いままアプリが動くことはない

## 既知の制約（対応済みだが把握しておくこと）
- **削除済みタスク・質問の扱い**は excludedAt（集計対象外の印）方式で恒久対応済み。削除する「前」に印を付けるので、後から推測しない。手動での除外／復帰もできる
- **感情検出のモデル読み込みに失敗しても、誰も気づけない**。参加者向けの「検出エラー」表示は撤去した（参加者には対処できず、進行も止まらないため）。調査者向けの通知は元々無い。実際の症状は「そのセッションだけ感情のグラフが出ない」。`track()` の送信先が未選定（本番では no-op）なので、計測を足すならプロバイダ選定とセットで
- 集計対象外（excludedAt）の残る制約: 自由回答（open）は「集計から外した項目」パネルに列挙されない（`aggregateScores` がスコア型しか扱わないため）。生きた自由回答に印が残る経路は塞いだので実害は無いが、削除した自由回答質問を画面から復帰させる導線は無い（CSV には残る）

## 次にやること（再開ポイント）
### ユーザー側の作業
- **実機での確認**（preview では検証不可の領域）: 小窓のフロー（録画→サービス→達成）、顔の見切れ警告、録画と会話ログ・感情グラフの同期
- インタビュー詳細の「文字起こしから回答を抽出する」を実行 → 比較テーブルが埋まるか、抽出内容が元の発言とズレていないか確認
- インタビュー詳細の「全件を再分析」を実行 → 英語の要約が日本語になるか（自動継続するので1回押すだけ）
- `.env.example` の `DAILY_API_KEY` の行を削除（Claude の権限設定で編集できなかった。機能影響なし）

### 未着手（優先度低）
- 残る lint 警告2件（`<img>` を next/image に、ref cleanup の助言）。動作影響なし
- 感情グラフの時刻ズレ: 2026-07-28 より前に録画されたセッションは、時間基準を揃える修正より前のデータなのでズレる可能性がある

## 注意（並行セッション・未コミット変更）
- なし（prisma の PostgreSQL 移行分はユーザー指示で 2026-07-27 に破棄済み）。

## 完了ログ（直近5件）
- 2026-07-29: 調査ページ最上部に結果サマリーを追加。集計を lib/interview-aggregate.ts に集約してサマリーと詳細の数字を一致させた。レビュー2巡で最小サンプル数・丸め・processing の取りこぼしを修正し、「削除済み判定」は退行のため撤回（PR #25）
- 2026-07-28: /kaizen 棚卸し。台帳の記録漏れ補完と、3回ルールで4件を昇格（新スキル ai-call-review / data-model-review、ship スキルへ2項目追記）。DECISIONS の実態と食い違う絶対ルールを訂正（PR #24）
- 2026-07-28: 残タスク6項目（一括再分析・prisma を db push に統一・デッドコード削除・AI設計の質問形式・lint 26件→0件・a11y）。PR #23
- 2026-07-28: セッション詳細のUI整理。会話ログ↔動画↔感情グラフの相互連動、上下2段レイアウト（グラフをシークバー化）、グラフ重複と不要な帯の撤去（PR #19〜#22）
- 2026-07-28: recharts 3 で activePayload 廃止によりグラフのシークが無効だった問題を修正。クリック座標から自前算出する方式に（PR #18）
- 2026-07-28: 過去セッションの回答を文字起こしからAI抽出＋AI要約の日本語化。比較テーブルが空だった原因の解消（PR #17）
- 2026-07-28: UXレビュー起点の P0〜P2 を実装。測定結果の構造化（成功率・NPS・所要時間）、感情判定の実データ化、全文検索、タグ付け/ハイライト、比較テーブル、調査の編集、SEQ、スクリーニング、パイロット実行（PR #11〜#16）
